DEF="../defconfig"
PAT="../patches"
UP="../patches/userpatches"
OUT4="../output/bcm2711"
OUT4v7="../output/bcm2711v7"
OUT3="../output/bcm2710"
OUT2="../output/bcm2709"
OUT="../output/bcm2708"
SRC="source"

KERNEL="linux-rpi"
MLKERNEL="linux"
KBUSER="marvin"
KBHOST="martian"

validation(){
if [[ `grep -w "builder=4.3" "userdata.txt"` ]]; then
	:;
else
	echo -e "${YLW}Userdata file is old! Please create a new one${FIN}."
	exit;
fi
}
# do not edit above this line

EXT4="3000MB"
BTRFS="2650MB"
XFS="2650MB"

# packages
PKG1="tzdata keyboard-configuration sudo man-db dbus initramfs-tools \
	fonty-rg patch curl wget apt-transport-https dirmngr cmake rsync \
	psmisc parted pv zip unzip aria2 libell0 fdisk i2c-tools"

PKG2="cpu hwinfo haveged git build-essential net-tools ifplugd crda \
	fuse3 wpasupplicant wireless-tools usbutils alsa-utils gettext \
	bison flex mc nano figlet toilet dialog python3 python3-setuptools \
	ntfs-3g bc zram-tools libncursesw5-dev libssl-dev autopoint autoconf \
	automake pkg-config libtool fake-hwclock avahi-utils distro-info-data \
	lsb-release liblzo2-2 bluetooth bluez bluez-tools gpiod python3-libgpiod"

# firmware
FIRMWARE="firmware-linux-nonfree firmware-linux firmware-misc-nonfree \
          firmware-realtek firmware-ralink"

# motd
BRAND="Raspberry Pi"
