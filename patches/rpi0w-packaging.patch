--- a/scripts/package/builddeb	2020-04-23 10:09:44.000000000 -0400
+++ b/scripts/package/builddeb	2020-04-23 18:04:16.014127259 -0400
@@ -92,6 +92,7 @@
 else
 	cp System.map "$tmpdir/boot/System.map-$version"
 	cp $KCONFIG_CONFIG "$tmpdir/boot/config-$version"
+	cp arch/arm/boot/zImage "$tmpdir/boot/kernel.img"
 fi
 cp "$($MAKE -s -f $srctree/Makefile image_name)" "$tmpdir/$installed_image_path"
 
@@ -139,6 +140,14 @@
 	mv $libc_headers_dir/usr/include/asm $libc_headers_dir/usr/include/$host_arch/
 fi
 
+# Install dtb and overlays
+mkdir -p "$tmpdir/boot/overlays"
+cp -r "$tmpdir/usr/lib/linux-image-$version/bcm2708-rpi-zero-w.dtb" "$tmpdir/boot/bcm2708-rpi-zero-w.dtb"
+cp -r "$tmpdir/usr/lib/linux-image-$version/bcm2708-rpi-zero.dtb" "$tmpdir/boot/bcm2708-rpi-zero.dtb"
+cp -r "$tmpdir/usr/lib/linux-image-$version/bcm2708-rpi-b.dtb" "$tmpdir/boot/bcm2708-rpi-b.dtb"
+cp -r "$tmpdir/usr/lib/linux-image-$version/bcm2708-rpi-b-plus.dtb" "$tmpdir/boot/bcm2708-rpi-b-plus.dtb"
+cp -r "$tmpdir/usr/lib/linux-image-$version/overlays/" "$tmpdir/boot/"
+
 # Install the maintainer scripts
 # Note: hook scripts under /etc/kernel are also executed by official Debian
 # kernel packages, as well as kernel packages built using make-kpkg.
